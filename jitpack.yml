# JitPack构建配置
jdk:
  - openjdk17
  
before_install:
  - sdk install java 17.0.7-tem
  - sdk use java 17.0.7-tem

install:
  - ./gradlew :client-a-sdk:clean :client-a-sdk:assembleRelease publishToMavenLocal

